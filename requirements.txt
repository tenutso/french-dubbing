# French Dubbing Pipeline v4.0 — Python Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: torch==2.8.0 and torchaudio==2.8.0 are PRE-INSTALLED on the RunPod
# pytorch:1.0.2-cu1281-torch280-ubuntu2404 image.
# If installing outside RunPod, add:
#   pip install torch==2.8.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128

# ── Audio / Video ──────────────────────────────────────────────────────────────
numpy>=1.26.4
scipy>=1.13.1
librosa>=0.10.2
soundfile>=0.12.1
pydub>=0.25.1

# ── Source Separation — Demucs ─────────────────────────────────────────────────
demucs>=4.0.0

# ── Transcription — faster-whisper (CTranslate2) ──────────────────────────────
faster-whisper>=1.1.0

# ── Speaker Diarization — pyannote.audio (optional, gated HF model) ───────────
pyannote.audio>=3.3.0

# ── Speaker Denoising (in priority order — first available is used) ────────────
# DeepFilterNet best quality; noisereduce and FFmpeg anlmdn are fallbacks.
deepfilternet
noisereduce>=3.0.0

# ── TTS — runs OUT-OF-PROCESS in its own venv ────────────────────────────────
# The TTS engine (XTTS, Chatterbox, …) is NOT installed in this env. It runs via
# tts/tts_worker.py inside /workspace/venvs/<engine>, built from the matching
# requirements-<engine>.txt (e.g. requirements-xtts.txt). This keeps each
# engine's conflicting pins (transformers/numpy/torch) out of the main env, so
# the main env below is free to track modern transformers and numpy.

# ── Subtitles ─────────────────────────────────────────────────────────────────
pysrt>=1.1.2

# ── Translation transport — Ollama HTTP ──────────────────────────────────────
requests>=2.32.0

# ── HuggingFace download acceleration ─────────────────────────────────────────
# Rust-based transfer client. Activated by HF_HUB_ENABLE_HF_TRANSFER=1.
hf_transfer>=0.1.6

# ── Web UI (FastAPI + uvicorn) ────────────────────────────────────────────────
# Launcher: bash 05_web.sh  → uvicorn web.app:app on 0.0.0.0:7860
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
# Vimeo URL ingestion in the web UI (downloads the source video before dubbing).
yt-dlp>=2024.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
click>=8.1.7
pyyaml>=6.0.1
packaging>=23.0
